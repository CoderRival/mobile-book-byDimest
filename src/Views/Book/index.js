import Front from './Front'
import Text from './Text'

const element = document.createElement('div')

element.appendChild(Front)
element.appendChild(Text)

const enter = () => {
    
}
const leave = () => {
    
}

export default { element, enter, leave }